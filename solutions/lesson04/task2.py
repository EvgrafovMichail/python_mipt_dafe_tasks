def merge_intervals(intervals: list[list[int, int]]) -> list[list[int, int]]:
    left = []
    right = []
    for i in intervals:
        left.append(i[0])
        right.append(i[1])
    left.sort()
    right.sort()
    for g in left:
        for     
    return [[0,0]]