import random
import math

# Параметры
p = 0.29
n = 791000  # количество испытаний
target_moe = 0.001  # 0.1 процентного пункта

# Проводим испытания
random.seed(123)  # для воспроизводимости
successes = 0
for _ in range(n):
    if random.random() <= p:
        successes += 1

# Результаты
frequency = successes / n
abs_diff_percent = abs(frequency - p) * 100  # в процентных пунктах
к
print(f"Количество испытаний: {n:,}")
print(f"Вероятность успеха: {p*100:.1f}%")
print(f"Успехов: {successes:,}")
print(f"Частота успехов: {frequency*100:.4f}%")
print(f"Отклонение от вероятности: {abs_diff_percent:.4f} процентных пункта")
print(f"Цель: отклонение < {target_moe*100:.4f} процентных пункта")
print(f"Уложились в цель? {abs_diff_percent < target_moe*100}")